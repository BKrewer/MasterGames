<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    >
<data>
    <variable name="categoria" type="com.devmobile.mastergames.model.Categoria" android:id="@+id/varCategoria"/>
</data>

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <Button
            android:id="@+id/categoryDetail"
            style="@style/Widget.AppCompat.Button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@{categoria.name}"
            android:textColor="#000000"
            android:textColorLink="#000000"
            android:textSize="18sp"
            android:onClick="CategoryDetail"
            android:tag="@{categoria.id}"
            android:background="#cfcfcf"/>
    </RelativeLayout>
</layout>